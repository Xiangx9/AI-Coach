{
  "bsonType": "object",
  "required": ["user_id", "duration", "calories", "status", "createTime"],
  "permission": { "read": "doc.user_id == auth.uid", "create": true, "update": "doc.user_id == auth.uid", "delete": "doc.user_id == auth.uid" },
  "properties": {
    "user_id": { "bsonType": "string" },
    "plan_id": { "bsonType": "string" },
    "duration": { "bsonType": "int" },
    "calories": { "bsonType": "int" },
    "status": { "bsonType": "bool" },
    "createTime": { "bsonType": "timestamp" }
  }
}
